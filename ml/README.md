# TF-IDF

```bash
python tf-idf.py
```

Example Output
```bash
Similarity between `I'd like an apple.` and `An apple a day keeps the doctor away.`: 0.0098
Similarity between `I'd like an apple.` and `Never compare an apple to an orange.`: 0.0289
Similarity between `I'd like an apple.` and `I prefer scikit-learn to orange.`: 0.0000
```
